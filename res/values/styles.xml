<resources>

    <style name="AppTheme" parent="android:Theme.Light">
        <item name="android:autoCompleteTextViewStyle">@style/WorkaroundAutoCompleteTextViewLight</item>
    </style>

    <!-- http://b.android.com/5237#c8 -->
    <style name="WorkaroundAutoCompleteTextViewLight" parent="@android:style/Widget.AutoCompleteTextView">
        <item name="android:textColor">@android:color/primary_text_light</item>
    </style>

    <style name="NoDisplayForDialogTheme" parent="android:Theme.NoDisplay"/>

</resources>